<% protocol = Rails.env.production? ? "https" : "http" %>
<%= link_to(image_tag("https://www.paypal.com/en_US/i/btn/btn_xpressCheckout.gif"), paypal_express_url(:payment_method_id => payment_method.id, :protocol => protocol), :method => :post, :id => "paypal_button") %>

<script>
  SpreePaypalExpress.paymentMethodID = "<%= payment_method.id %>"
 </script>
